# Dashboard Warmlink - Automatyczne wykrywanie encji
# Wymaga karty auto-entities z HACS: https://github.com/thomasloven/lovelace-auto-entities
#
# Instalacja auto-entities:
# 1. HACS → Frontend → Search "auto-entities" → Install
# 2. Restart Home Assistant
# 3. Skopiuj ten dashboard
#
# UWAGA: Dashboard używa filtra "integration: warmlink" z dodatkowymi wzorcami
# opartymi na nazwach encji (translation_key), nie na pełnym entity_id

title: Pompa Ciepła Warmlink
views:
  # ============================================================================
  # PRZEGLĄD
  # ============================================================================
  - title: Przegląd
    path: przeglad
    icon: mdi:heat-pump
    cards:
      # Główne sterowanie - Climate i Water Heater
      - type: custom:auto-entities
        card:
          type: entities
          title: "🎛️ Sterowanie"
        filter:
          include:
            - domain: climate
              integration: warmlink
            - domain: water_heater
              integration: warmlink
            - domain: switch
              integration: warmlink
              name: "/(Power|Zasilanie)/"
            - domain: select
              integration: warmlink
              name: "/(Mode|Tryb pracy)/"

      # Status - binary sensors
      - type: custom:auto-entities
        card:
          type: glance
          title: "📊 Status"
          columns: 4
        filter:
          include:
            - domain: binary_sensor
              integration: warmlink

      # Temperatury główne T01-T04, T08
      - type: custom:auto-entities
        card:
          type: glance
          title: "🌡️ Temperatury główne"
          columns: 4
        filter:
          include:
            - domain: sensor
              integration: warmlink
              name: "/\\(T0[1248]\\)/"

      # Setpointy główne R01, R02, R03, R70
      - type: custom:auto-entities
        card:
          type: entities
          title: "🎯 Temperatury zadane"
        filter:
          include:
            - domain: number
              integration: warmlink
              name: "/\\(R0[123]\\)|\\(R70\\)/"

      # Wydajność - Power, Capacity, COP
      - type: custom:auto-entities
        card:
          type: glance
          title: "⚡ Wydajność"
          columns: 4
        filter:
          include:
            - domain: sensor
              integration: warmlink
              name: "/(Power In|Capacity Out|COP|Pobór|Moc|Współczynnik)/"
            - domain: sensor
              integration: warmlink
              name: "/\\(T30\\)/"

  # ============================================================================
  # T - TEMPERATURY
  # ============================================================================
  - title: T - Temperatury
    path: t-temperatury
    icon: mdi:thermometer
    cards:
      # Wszystkie sensory T01-T09
      - type: custom:auto-entities
        card:
          type: entities
          title: "💧 Temperatury wody (T01-T09)"
        filter:
          include:
            - domain: sensor
              integration: warmlink
              name: "/\\(T0[1-9]\\)/"
        sort:
          method: name

      # Temperatury T10-T19
      - type: custom:auto-entities
        card:
          type: entities
          title: "🌡️ Temperatury (T10-T19)"
        filter:
          include:
            - domain: sensor
              integration: warmlink
              name: "/\\(T1[0-9]\\)/"
        sort:
          method: name

      # Wentylator T27-T29
      - type: custom:auto-entities
        card:
          type: entities
          title: "🌀 Wentylator (T27-T29)"
        filter:
          include:
            - domain: sensor
              integration: warmlink
              name: "/\\(T2[7-9]\\)/"
        sort:
          method: name

      # Sprężarka i elektryka T30-T39
      - type: custom:auto-entities
        card:
          type: entities
          title: "⚡ Elektryka (T30-T39)"
        filter:
          include:
            - domain: sensor
              integration: warmlink
              name: "/\\(T3[0-9]\\)/"
        sort:
          method: name

      # Pozostałe T40+
      - type: custom:auto-entities
        card:
          type: entities
          title: "🔧 Pozostałe (T40+)"
        filter:
          include:
            - domain: sensor
              integration: warmlink
              name: "/\\(T[4-9][0-9]\\)/"
        sort:
          method: name

      # Indoor sensors DP
      - type: custom:auto-entities
        card:
          type: entities
          title: "🏠 Klimat wewnętrzny"
        filter:
          include:
            - domain: sensor
              integration: warmlink
              name: "/(Indoor|Humidity|Dew Point|Wewnętrzn|Wilgotność|Punkt rosy)/"

  # ============================================================================
  # R - SETPOINTS
  # ============================================================================
  - title: R - Setpoints
    path: r-setpoints
    icon: mdi:target
    cards:
      # Number entities (edytowalne) - R01-R99
      - type: custom:auto-entities
        card:
          type: entities
          title: "🎯 Temperatury zadane (edytowalne)"
        filter:
          include:
            - domain: number
              integration: warmlink
              name: "/\\(R[0-9]+\\)/"
        sort:
          method: name

      # Sensor entities (odczyty) - R sensory
      - type: custom:auto-entities
        card:
          type: entities
          title: "📊 Odczyty setpointów"
        filter:
          include:
            - domain: sensor
              integration: warmlink
              name: "/\\(R[0-9]+\\)/"
        sort:
          method: name

  # ============================================================================
  # H - SYSTEM
  # ============================================================================
  - title: H - System
    path: h-system
    icon: mdi:cog
    cards:
      # Switches H
      - type: custom:auto-entities
        card:
          type: entities
          title: "🔘 Funkcje systemu (H - przełączniki)"
        filter:
          include:
            - domain: switch
              integration: warmlink
              name: "/\\(H[0-9]+\\)/"
        sort:
          method: name

      # Selects H
      - type: custom:auto-entities
        card:
          type: entities
          title: "📝 Ustawienia systemu (H - wybór)"
        filter:
          include:
            - domain: select
              integration: warmlink
              name: "/\\(H[0-9]+\\)/"
        sort:
          method: name

      # Numbers H
      - type: custom:auto-entities
        card:
          type: entities
          title: "🎚️ Parametry systemu (H - wartości)"
        filter:
          include:
            - domain: number
              integration: warmlink
              name: "/\\(H[0-9]+\\)/"
        sort:
          method: name

  # ============================================================================
  # C - SPRĘŻARKA
  # ============================================================================
  - title: C - Sprężarka
    path: c-sprezarka
    icon: mdi:cog-sync
    cards:
      - type: custom:auto-entities
        card:
          type: entities
          title: "⚙️ Sprężarka (C)"
        filter:
          include:
            - integration: warmlink
              name: "/\\(C[0-9]+\\)/"
        sort:
          method: name

  # ============================================================================
  # D - ODSZRANIANIE
  # ============================================================================
  - title: D - Odszranianie
    path: d-odszranianie
    icon: mdi:snowflake-melt
    cards:
      - type: custom:auto-entities
        card:
          type: entities
          title: "❄️ Odszranianie (D)"
        filter:
          include:
            - domain: binary_sensor
              integration: warmlink
              name: "/(Defrost|Odszranianie)/"
            - integration: warmlink
              name: "/\\(D[0-9]+\\)/"
        sort:
          method: name

  # ============================================================================
  # G - DEZYNFEKCJA
  # ============================================================================
  - title: G - Dezynfekcja
    path: g-dezynfekcja
    icon: mdi:shield-bug
    cards:
      - type: custom:auto-entities
        card:
          type: entities
          title: "🦠 Dezynfekcja Anti-Legionella (G)"
        filter:
          include:
            - integration: warmlink
              name: "/\\(G[0-9]+\\)/"
        sort:
          method: name

  # ============================================================================
  # A - OCHRONA
  # ============================================================================
  - title: A - Ochrona
    path: a-ochrona
    icon: mdi:shield-check
    cards:
      - type: custom:auto-entities
        card:
          type: entities
          title: "🛡️ Parametry ochrony (A)"
        filter:
          include:
            - integration: warmlink
              name: "/\\(A[0-9]+\\)/"
        sort:
          method: name

  # ============================================================================
  # F - WENTYLATOR
  # ============================================================================
  - title: F - Wentylator
    path: f-wentylator
    icon: mdi:fan
    cards:
      - type: custom:auto-entities
        card:
          type: entities
          title: "🌀 Wentylator (F)"
        filter:
          include:
            - integration: warmlink
              name: "/\\(F[0-9]+\\)/"
        sort:
          method: name

  # ============================================================================
  # E - EEV
  # ============================================================================
  - title: E - EEV
    path: e-eev
    icon: mdi:valve
    cards:
      - type: custom:auto-entities
        card:
          type: entities
          title: "🔧 Zawór rozprężny EEV (E)"
        filter:
          include:
            - integration: warmlink
              name: "/\\(E[0-9]+\\)/"
        sort:
          method: name

  # ============================================================================
  # P - POMPA
  # ============================================================================
  - title: P - Pompa
    path: p-pompa
    icon: mdi:pump
    cards:
      - type: custom:auto-entities
        card:
          type: entities
          title: "💧 Pompa obiegowa (P)"
        filter:
          include:
            - integration: warmlink
              name: "/\\(P[0-9]+\\)/"
            - domain: sensor
              integration: warmlink
              name: "/\\(T39\\)|(Flow|Przepływ)/"
        sort:
          method: name

  # ============================================================================
  # Z - STREFY
  # ============================================================================
  - title: Z - Strefy
    path: z-strefy
    icon: mdi:home-floor-1
    cards:
      - type: custom:auto-entities
        card:
          type: entities
          title: "🏠 Strefy grzewcze (Z)"
        filter:
          include:
            - integration: warmlink
              name: "/\\(Z[0-9]+\\)|Zone|Strefa/"
        sort:
          method: name

  # ============================================================================
  # ENERGIA
  # ============================================================================
  - title: Energia
    path: energia
    icon: mdi:lightning-bolt
    cards:
      - type: custom:auto-entities
        card:
          type: entities
          title: "⚡ Wydajność bieżąca"
        filter:
          include:
            - domain: sensor
              integration: warmlink
              name: "/(Power|Capacity|COP|EER|Pobór|Moc|Wydajność|Współczynnik)/"
        sort:
          method: name

      - type: custom:auto-entities
        card:
          type: entities
          title: "📊 Zużycie energii"
        filter:
          include:
            - domain: sensor
              integration: warmlink
              name: "/(Heating|Cooling|DHW|Consumption|Grzanie|Chłodzenie|Zużycie)/"
              attributes:
                device_class: energy
        sort:
          method: name

  # ============================================================================
  # INFO
  # ============================================================================
  - title: Info
    path: info
    icon: mdi:information
    cards:
      - type: custom:auto-entities
        card:
          type: entities
          title: "ℹ️ Wersje oprogramowania"
        filter:
          include:
            - domain: sensor
              integration: warmlink
              name: "/(Version|Wersja)/"

      - type: custom:auto-entities
        card:
          type: entities
          title: "📊 Status trybu"
        filter:
          include:
            - domain: sensor
              integration: warmlink
              name: "/(Mode State|Stan trybu)/"

  # ============================================================================
  # WSZYSTKIE ENCJE
  # ============================================================================
  - title: Wszystkie
    path: wszystkie
    icon: mdi:format-list-bulleted
    cards:
      - type: custom:auto-entities
        card:
          type: entities
          title: "📋 Wszystkie encje Warmlink"
        filter:
          include:
            - integration: warmlink
        sort:
          method: name
